# better-do-:]  
project made when i was bored, so, This tool (is a chrome extension) changes :), :3 into :]  
# Installation
go to `chrome://extensions`, enable developer mode, go to Load Unpacked, select the folder downloaded, boom  
#Update Installation
## if you installed via git
do `git pull origin master` or `git pull --rebase origin master`  
## if you install3d via download from web
simply redownload and replace the folder  
